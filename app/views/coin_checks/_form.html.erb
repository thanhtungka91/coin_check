<%= form_for(coin_check) do |f| %>
  <% if coin_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coin_check.errors.count, "error") %> prohibited this coin_check from being saved:</h2>

      <ul>
      <% coin_check.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
